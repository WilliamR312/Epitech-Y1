/*
** EPITECH PROJECT, 2020
** MY_ISNEG
** File description:
** Returns 1 if nb is negative and 0 if it's positive.
*/

int my_isneg(int n)
{
    if (n < 0)
        return (1);
    else
        return (0);
    return (2);
}