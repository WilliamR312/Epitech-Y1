/*
** EPITECH PROJECT, 2020
** GET_SEPARATOR_TAB
** File description:
** Get separators tab
*/

#include "my.h"
#include "mysh.h"
#include "struct.h"

int *get_separator_tab(char *str)
{
    int *tab = NULL;
    int nbr_sep = 0;

    for (int i = 0; str[i]; i++) {
        if (str[i] == ';' || str[i] == '|')
    }
    return (0);
}